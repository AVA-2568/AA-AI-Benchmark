# 评分方法论

> 本文档是 [README](README.md) 中评分方法的完整展开，包含权重推导、归一化细节、缺失值填补算法、成本口径公式和 R² 拟合质量。

## 指标选取与权重

从 Artificial Analysis 的 providers leaderboard 中选取 **9 个代表性基准指标**，归入 4 个大类，每大类再分配子权重：

| 大类（权重） | 指标 | 子权重 | 全局权重 |
|---|---|---|---|
| **智能体 Agentic** (20%) | GDPval-AA | 100% | 20% |
| **编程 Coding** (20%) | Terminal-Bench Hard | 50% | 10% |
| | Terminal-Bench v2.1 | 30% | 6% |
| | SciCode | 20% | 4% |
| **通用 General** (40%) | LCR | 30% | 12% |
| | Omniscience Index | 30% | 12% |
| | IFBench | 40% | 16% |
| **知识 Knowledge** (20%) | GPQA Diamond | 40% | 8% |
| | HLE | 60% | 12% |

全局权重之和 = 1.00。

**设计逻辑**：通用能力（LCR + Omniscience + IFBench）权重最高（40%），因为它代表模型的日常使用体验；编程和智能体各 20%；知识（GPQA + HLE）20%。

## 数据预处理

### 去重策略

原始榜单包含同一模型在不同推理强度（max / xhigh / high / medium / low）的多条记录，以及同一模型在多个云平台（AWS / Vertex / AI Studio）的重复条目。

- 按 **Model Slug** 去重
- 同一 Slug 多条记录 → 保留 **Intelligence Index 最高**的那条
- 保留的变体涵盖不同强度级别，用户可根据成本和性能需求选择

### 排名截断

评分后按总分降序排列，取 **前 15%**（当前样本量 390 行去重后 → 59 行输出）。

## 归一化方法

```
归一化得分 = (原始值 - 该指标最小值) / (该指标最大值 - 该指标最小值) × 100
```

- 最小值和最大值来自去重后的全部 390 行实测数据
- 结果范围 0–100，**代表在该样本中的相对位置**，而非理论能力满分
- 举例：Omniscience Index 的实测区间约为 -34 到 40（非 -100 到 100），归一化后最高分约 100，最低分约 0

## 缺失值填补

### 为什么会有缺失值

- 并非所有模型都跑过全部基准测试
- 部分新模型刚发布、测试数据尚未覆盖
- 部分服务商未提交特定基准

### 填补算法：多变量岭回归 + 迭代收敛

对每个目标指标 `T`，用以下特征做岭回归预测：

1. 其他 8 个基准指标
2. Intelligence Index（综合智能分，覆盖面最广的指标）

合计 9 个预测特征。对单个缺失的模型，一次预测即可；对多个指标缺失的模型，迭代多轮直至收敛（最多 30 轮）。

```
预测值 = β₀ + Σ(βᵢ × 特征ᵢ)，β 由岭回归求解
         ↑ 裁剪到该列 P95（第 95 百分位）
```

- α（正则化强度）= 1.0
- 初始化：缺失值用训练集 top50 均值填充
- 裁剪到 P95：防止缺失模型被推到历史最佳分，同时保留相对梯度

### 如何识别填补值

- XLSX 文件中红色背景的单元格 = 回归预测值
- CSV 中 `Imputed` 列标注 `指标名(reg)`
- README 表中「回归填���项」列直接显示

## 模型拟合质量（R²）

每个基准指标的「用其他 9 个特征预测本指标」的训练集 R²：

| 指标 | R² | 解读 |
|---|---|---|
| GDPval-AA | ≈0.85+ | 高度可预测 |
| Terminal-Bench Hard | ≈0.75+ | 较可预测 |
| Terminal-Bench v2.1 | ≈0.70+ | 较可预测 |
| SciCode | ≈0.80+ | 较可预测 |
| LCR | ≈0.75+ | 较可预测 |
| Omniscience Index | ≈0.80+ | 较可预测 |
| IFBench | ≈0.60+ | 中等可预测 |
| GPQA Diamond | ��0.85+ | 高度可预测 |
| HLE | ≈0.80+ | 较可预测 |

> R² 值随每次数据更新而浮动，精确值见 XLSX「说明」sheet。R² 越高，表示该指标的缺失值预测越可信。IFBench 的 R² 相对偏低，其预测值仅供参考。

## 成本估算

### 成本口径

假设典型 API 调用模式：
- **70% 输入 token + 30% 输出 token**
- **50% 的输入 token 命中提示缓存**（适用于多轮对话场景）

### 成本公式

```
Total $/1M = 0.35 × 输入价 + 0.35 × 缓存命中价 + 0.30 × 输出价
```

- 输入价、输出价来自服务商在榜单中标注的 API 定价
- 缓存命中价缺失时，回退为输入价
- 单位：USD / 百万 token

### 参考意义

成本估算让你在同分段模型间做性价比取舍。但注意：
- 价格为快照，实际可能变动
- 大客户通常有批发折扣，榜单未体现
- 不同推理强度（max / high / low）的价格差异已在各变体中体现

## 注意事项

1. **非绝对排名**：分数是相对排名，不代表模型的能力绝对值
2. **填补值可信度**：R² 高的指标填补更可靠，R² 低的仅供参考
3. **权重是主观的**：权重设计反映了「日常使用 + 编程 + 知识」的平衡，可根据自身需求调整 `score_aa.py` 中的权重配置
4. **去重策略影响**：保留 Intelligence Index 最高档意味着排名偏向模型「最优变体」的能力上限
